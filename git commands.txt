1. cd to file
2. git status
3. git add .
4. git commit -m "comment"
5. git push origin master

*git remote -v : check repo link
*git rm origin : remove repo link
*git add origin <link repo>

*clone file from github
forked: git clone <link ssh>